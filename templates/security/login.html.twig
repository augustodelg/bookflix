{% extends 'base.html.twig' %}

{% block title %}Bookflix - Iniciar sesion!{% endblock %}

{% block body %}
<style>
body{
      background: #eee;
}
</style>
    <div class="col-xl-12 col-sm-12" style="margin-top:-3%">
    <img src="{{ asset('logo.svg', 'images') }}" class=" img-fluid rounded mx-auto d-block" height="200" width="800" alt="Bookflix" id="logo" style= "padding: 8%;" >
    </div>
<div class="container">
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% endif %}
    <div class="container" style="transform: scale(0.9)">
    <div class="container"id = "top-box">
        <h1 class="h3 mb-3 font-weight-normal" id = "text-box" >Iniciar sesion</h1>
    </div>
    
        <div class="container" id = "end-box" >
        <label style="font-family:Asap Condensed" for="inputEmail">Email</label>
        <input  type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
        <br>
        <label style="font-family:Asap Condensed" for="inputPassword">Password</label>
        <input type="password" name="password" id="inputPassword" class="form-control" required>
        </div>
    
    
    

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

    <br>

    <button class="btn btn-lg btn-primary" type="submit">
        Entrar
    </button>
</form>
</div>
{% endblock %}
